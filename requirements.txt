fastapi>=0.115,<1
uvicorn[standard]>=0.30
sqlalchemy>=2.0,<2.1
alembic>=1.13,<2
pydantic>=2.7,<3
pydantic-settings>=2.4,<3
tzdata>=2024.1

# Postgres driver. On Linux hosts (Render) the binary package bundles libpq.
# On Windows it is left out: Smart App Control blocks its unsigned DLLs, so
# psycopg loads the system libpq instead (see README).
psycopg[binary]>=3.2,<4 ; sys_platform != "win32"
psycopg>=3.2,<4 ; sys_platform == "win32"

# Development
pytest>=8
httpx2>=2.13
